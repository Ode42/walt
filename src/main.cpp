#include <iostream>
#include <string>
#include "task.h"

int main()
{
    std::cout << "WALT" << std::endl;
    while (true) // while program is running, run task function in loop
    {
        task();
    }
}